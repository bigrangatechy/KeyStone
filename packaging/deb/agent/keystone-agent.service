# SPDX-FileCopyrightText: 2026 The KeyStone Authors
# SPDX-License-Identifier: GPL-2.0-or-later

[Unit]
Description=KeyStone agent (unlimited-node monitoring)
Documentation=https://git.bigrangatech.com/Ranga/keystone
After=network-online.target docker.socket
Wants=network-online.target
# docker is optional. After=docker.socket waits for the socket when Engine
# is installed. Never Requires=/BindsTo=/PartOf= docker: a KeyStone upgrade
# must not stop the engine or containers.

[Service]
Type=simple
User=keystone
Group=keystone
EnvironmentFile=-/etc/default/keystone-agent
ExecStart=/usr/bin/keystone-agent --config /etc/keystone/agent.toml
Restart=on-failure
RestartSec=5
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
PrivateTmp=true
# /run/keystone must be the directory (not -/run/keystone/sys.sock).
# Ignore-if-missing on the sock file leaves /run read-only when the
# helper is enabled after the agent has already started.
RuntimeDirectory=keystone
RuntimeDirectoryPreserve=yes
ReadWritePaths=/var/lib/keystone -/run/docker.sock -/var/run/docker.sock /run/keystone

[Install]
WantedBy=multi-user.target
