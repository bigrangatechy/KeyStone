# SPDX-FileCopyrightText: 2026 The KeyStone Authors
# SPDX-License-Identifier: GPL-2.0-or-later

# Example KeyStone agent config for a local smoke test (writable without root).
# Packaged installs use /var/lib/keystone/agent-buffer.
# Run `cargo run` from the repo root.
#
# Required: ingest_url, ingest_token, node_id (or hostname), buffer_dir.
# Optional docker.host if the engine socket is not /var/run/docker.sock.
# Optional tls_ca_file when ingest_url is https:// and the CA is private.
# Poll interval, Docker enable/manage/exec, labels, and compose paths are
# node Settings once connected.

ingest_url = "http://127.0.0.1:9100"
ingest_token = "change-me"
# node_id defaults to hostname when omitted
buffer_dir = ".smoke/agent-buffer"
# tls_ca_file = "tls/ca.pem"  # only when ingest_url is https:// (private CA)

# docker.host = "unix:///var/run/docker.sock"
