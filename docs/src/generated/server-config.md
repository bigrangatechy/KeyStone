<!-- Generated by cargo xtask docs / keystone-core::docs. Do not edit. -->

# Server configuration

Fields come from the Rust config structs (`schemars`).

Server configuration. Source of truth for `docs/src/generated/server-config.md`.

| Field | Type | Description |
|---|---|---|
| `auth` | object |  |
| `data_dir` | string | Data directory for SQLite, sessions, and the series store. |
| `grpc_listen` | string | gRPC ingest listen address. |
| `http_listen` | string | HTTP UI and API listen address. |
| `ingest_token` | string | Shared ingest token. Agents must present this. It cannot mutate Docker. |
| `prometheus_scrape` | array of PrometheusScrape |  |
| `retention_hours` | integer | Series retention in hours. |
| `snmp_scrape` | array of SnmpScrape |  |

## `PrometheusScrape`

| Field | Type | Description |
|---|---|---|
| `interval_secs` | integer |  |
| `name` | string | Job name stored as the node_id if `node_id` is empty. |
| `node_id` | string | Optional node id to attach samples to. |
| `url` | string | Exposition URL, for example `http://127.0.0.1:9100/metrics`. |

## `ServerAuth`

| Field | Type | Description |
|---|---|---|
| `password_hash` | string | Argon2id password hash. If empty, `KEYSTONE_ADMIN_PASSWORD` is hashed on first start. |
| `username` | string | Local admin username. |

## `SnmpScrape`

| Field | Type | Description |
|---|---|---|
| `community` | string |  |
| `interval_secs` | integer |  |
| `name` | string |  |
| `node_id` | string |  |
| `target` | string | `host:port` (port defaults to 161 if omitted). |
